# 用户登录系统设计方案

## 📋 目录
1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术方案选择](#技术方案选择)
4. [系统架构设计](#系统架构设计)
5. [数据库设计](#数据库设计)
6. [UI/UX设计](#uiux设计)
7. [实现步骤](#实现步骤)
8. [安全性考虑](#安全性考虑)
9. [后期扩展](#后期扩展)

---

## 概述

### 项目背景
当前Photoshop Demo应用已实现了完整的图片编辑功能，包括：
- 基础调整（亮度、对比度、饱和度）
- 裁切和旋转
- **滤镜功能**（LUT滤镜）
- 美化功能
- 拼图功能
- 项目管理

### 新增需求
**添加用户登录系统，只有登录后才能使用滤镜功能。**

### 设计目标
- ✅ 实现完整的用户注册、登录、注销流程
- ✅ 限制未登录用户访问滤镜功能
- ✅ 保持良好的用户体验
- ✅ 确保数据安全性
- ✅ 为后续云端功能预留扩展空间

---

## 功能需求

### 核心功能

#### 1. 用户注册
- 用户名注册（唯一性校验）
- 密码设置（强度要求）
- 邮箱绑定（可选）
- 注册成功后自动登录

#### 2. 用户登录
- 用户名/邮箱登录
- 密码验证
- 记住我功能（自动登录）
- 登录状态持久化

#### 3. 用户注销
- 退出登录
- 清除本地会话
- 可选：清除用户数据

#### 4. 权限控制
- **未登录状态**：
  - ✅ 可以使用基础调整功能
  - ✅ 可以使用裁切、旋转功能
  - ✅ 可以使用美化功能
  - ❌ **不能使用滤镜功能**（点击时提示登录）
  - ✅ 可以使用拼图功能
  
- **已登录状态**：
  - ✅ 所有功能完全开放

#### 5. 会话管理
- 登录状态检查
- Token过期处理
- 自动登录恢复

---

## 技术方案选择

### 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| **方案1：本地存储** | ✅ 实现简单<br>✅ 无需服务器<br>✅ 响应快速 | ❌ 无法多设备同步<br>❌ 数据易丢失 | 个人单机使用 |
| **方案2：Firebase** | ✅ 开箱即用<br>✅ 多设备同步<br>✅ 免费额度充足 | ❌ 需要Google服务<br>❌ 国内访问受限 | 国际版应用 |
| **方案3：自建后端** | ✅ 完全可控<br>✅ 高度定制<br>✅ 无第三方依赖 | ❌ 开发成本高<br>❌ 需要服务器维护 | 商业级应用 |

### 推荐方案：**阶段化实现**

#### 第一阶段：本地存储（当前实现）
使用 **SQLite + SharedPreferences**
- 快速实现MVP
- 验证用户体验
- 无需服务器成本

#### 第二阶段：云端升级（未来扩展）
迁移至 **Firebase Authentication**
- 多设备同步
- 社交账号登录
- 云端项目存储

---

## 系统架构设计

### 整体架构

```
┌─────────────────────────────────────────────┐
│            应用层 (UI Layer)                 │
├─────────────────────────────────────────────┤
│  LoginActivity  │  RegisterActivity │ ...   │
│  HomeActivity   │  EditActivity     │       │
└──────────────┬──────────────────────────────┘
               │
┌──────────────▼──────────────────────────────┐
│         业务逻辑层 (Business Layer)          │
├─────────────────────────────────────────────┤
│  UserManager    - 用户管理                   │
│  SessionManager - 会话管理                   │
│  AuthGuard      - 权限控制                   │
└──────────────┬──────────────────────────────┘
               │
┌──────────────▼──────────────────────────────┐
│         数据层 (Data Layer)                  │
├─────────────────────────────────────────────┤
│  UserDatabase (SQLite)  - 用户数据库         │
│  SharedPreferences      - 会话存储           │
│  EncryptionHelper       - 加密工具           │
└─────────────────────────────────────────────┘
```

### 核心类设计

#### 1. User（用户实体类）
```java
public class User {
    private String userId;          // 用户ID（UUID）
    private String username;        // 用户名（唯一）
    private String passwordHash;    // 密码哈希
    private String email;           // 邮箱（可选）
    private long createdAt;         // 创建时间
    private long lastLoginAt;       // 最后登录时间
    private String avatarPath;      // 头像路径（可选）
}
```

#### 2. UserManager（用户管理器）
```java
public class UserManager {
    // 用户注册
    public Result<User> register(String username, String password, String email);
    
    // 用户登录
    public Result<User> login(String username, String password);
    
    // 检查用户名是否存在
    public boolean isUsernameExists(String username);
    
    // 更新用户信息
    public boolean updateUser(User user);
    
    // 密码重置
    public boolean resetPassword(String username, String newPassword);
}
```

#### 3. SessionManager（会话管理器）
```java
public class SessionManager {
    // 创建会话
    public void createSession(User user, boolean rememberMe);
    
    // 检查登录状态
    public boolean isLoggedIn();
    
    // 获取当前用户
    public User getCurrentUser();
    
    // 注销登录
    public void logout();
    
    // 自动登录恢复
    public User restoreSession();
}
```

#### 4. AuthGuard（权限守卫）
```java
public class AuthGuard {
    // 检查滤镜功能权限
    public static boolean checkFilterPermission(Context context);
    
    // 显示登录提示
    public static void showLoginPrompt(Context context);
    
    // 导航到登录页面
    public static void navigateToLogin(Context context);
}
```

#### 5. UserDatabase（用户数据库）
```java
public class UserDatabase extends SQLiteOpenHelper {
    // 用户表操作
    public long insertUser(User user);
    public User getUserByUsername(String username);
    public User getUserById(String userId);
    public boolean updateUser(User user);
    public boolean deleteUser(String userId);
}
```

---

## 数据库设计

### SQLite表结构

#### users 表
```sql
CREATE TABLE users (
    user_id TEXT PRIMARY KEY,           -- UUID
    username TEXT UNIQUE NOT NULL,      -- 用户名
    password_hash TEXT NOT NULL,        -- 密码哈希（BCrypt）
    email TEXT,                         -- 邮箱
    avatar_path TEXT,                   -- 头像路径
    created_at INTEGER NOT NULL,        -- 创建时间（时间戳）
    last_login_at INTEGER,              -- 最后登录时间
    is_active INTEGER DEFAULT 1         -- 账号状态（0=禁用，1=启用）
);

-- 索引
CREATE INDEX idx_username ON users(username);
CREATE INDEX idx_email ON users(email);
```

### SharedPreferences存储

#### session_prefs.xml
```xml
<!-- 会话信息 -->
<string name="current_user_id">用户ID</string>
<string name="session_token">会话令牌</string>
<long name="session_created_at">会话创建时间</long>
<boolean name="remember_me">记住登录状态</boolean>
<boolean name="auto_login_enabled">自动登录开关</boolean>
```

---

## UI/UX设计

### 界面流程图

```
           启动应用
              │
              ▼
      ┌─────────────┐
      │ HomeActivity│
      └──────┬──────┘
             │
             ├─→ 已登录？──→ 正常使用所有功能
             │
             └─→ 未登录
                   │
                   ▼
            点击滤镜功能
                   │
                   ▼
         ┌──────────────────┐
         │  登录提示对话框    │
         │  "滤镜功能需要登录" │
         └────┬─────────┬────┘
              │         │
         [立即登录]  [稍后]
              │         │
              ▼         └→ 返回
      LoginActivity
              │
              ├─→ 登录成功 ──→ 返回EditActivity，开启滤镜
              │
              └─→ 没有账号？──→ RegisterActivity
```

### 1. LoginActivity（登录页面）

#### 布局元素
```
┌─────────────────────────────────┐
│         Photoshop Demo          │
│         ━━━━━━━━━━━            │
│                                 │
│  👤 用户名/邮箱                  │
│  ┌─────────────────────────┐   │
│  │                         │   │
│  └─────────────────────────┘   │
│                                 │
│  🔒 密码                        │
│  ┌─────────────────────────┐   │
│  │         ••••••          │   │
│  └─────────────────────────┘   │
│                                 │
│  ☑ 记住我          忘记密码？   │
│                                 │
│  ┌─────────────────────────┐   │
│  │      登  录              │   │
│  └─────────────────────────┘   │
│                                 │
│  还没有账号？ 立即注册           │
│                                 │
│  ─────── 或者 ───────           │
│  [Google登录]  [游客模式]       │
└─────────────────────────────────┘
```

#### 功能说明
- **输入验证**：实时检查用户名和密码格式
- **错误提示**：友好的错误信息（用户名不存在、密码错误等）
- **记住我**：保存登录状态，下次自动登录
- **忘记密码**：密码重置流程
- **快捷注册**：直接跳转到注册页面
- **游客模式**：临时跳过登录（功能受限）

### 2. RegisterActivity（注册页面）

#### 布局元素
```
┌─────────────────────────────────┐
│         创建账号                 │
│                                 │
│  👤 用户名                       │
│  ┌─────────────────────────┐   │
│  │                         │   │
│  └─────────────────────────┘   │
│  ✓ 用户名可用                    │
│                                 │
│  📧 邮箱（可选）                 │
│  ┌─────────────────────────┐   │
│  │                         │   │
│  └─────────────────────────┘   │
│                                 │
│  🔒 密码                        │
│  ┌─────────────────────────┐   │
│  │         ••••••          │   │
│  └─────────────────────────┘   │
│  密码强度：████████░░ 强        │
│                                 │
│  🔒 确认密码                    │
│  ┌─────────────────────────┐   │
│  │         ••••••          │   │
│  └─────────────────────────┘   │
│                                 │
│  ☑ 我同意《用户协议》和《隐私政策》│
│                                 │
│  ┌─────────────────────────┐   │
│  │      注  册              │   │
│  └─────────────────────────┘   │
│                                 │
│  已有账号？ 立即登录             │
└─────────────────────────────────┘
```

#### 功能说明
- **用户名检查**：实时验证用户名是否已存在
- **密码强度检测**：可视化显示密码强度
- **密码确认**：确保两次输入一致
- **邮箱验证**：格式校验（可选功能）
- **协议勾选**：必须同意才能注册

### 3. 用户中心（个人信息页）

#### 布局元素
```
┌─────────────────────────────────┐
│  ←  个人中心                     │
├─────────────────────────────────┤
│         ┌─────┐                 │
│         │ 头像 │                 │
│         └─────┘                 │
│       用户名：photolover         │
│       邮箱：user@example.com     │
│       注册时间：2024-01-15       │
│                                 │
├─────────────────────────────────┤
│  账号设置                        │
│  ├─ 修改密码          >         │
│  ├─ 修改头像          >         │
│  └─ 绑定邮箱          >         │
│                                 │
│  偏好设置                        │
│  ├─ 自动登录      [开关]        │
│  └─ 云端同步      [开关]        │
│                                 │
│  其他                           │
│  ├─ 关于我们          >         │
│  ├─ 隐私政策          >         │
│  └─ 使用条款          >         │
│                                 │
│  ┌─────────────────────────┐   │
│  │      退出登录            │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

### 4. 登录提示对话框

#### 设计
```
┌─────────────────────────────────┐
│         🔒                       │
│    该功能需要登录                 │
│                                 │
│  滤镜功能仅对登录用户开放，       │
│  登录后即可使用所有高级滤镜效果。 │
│                                 │
│  ┌────────────┐ ┌────────────┐ │
│  │   稍后登录  │ │  立即登录  │ │
│  └────────────┘ └────────────┘ │
└─────────────────────────────────┘
```

### 5. HomeActivity 改造

#### 添加用户状态显示
```
┌─────────────────────────────────┐
│  Photoshop Demo    👤 [登录]     │  ← 未登录状态
│                                 │
│  或                              │
│                                 │
│  Photoshop Demo    😊 用户名 ▼   │  ← 已登录状态
│                                 │     点击显示下拉菜单
│  ┌─────────────────────────┐   │
│  │  项目列表...             │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

### 6. EditActivity 改造

#### 滤镜按钮状态
```
未登录状态：
┌──────────────┐
│  滤镜 🔒      │  ← 带锁图标，点击弹出登录提示
└──────────────┘

已登录状态：
┌──────────────┐
│  滤镜         │  ← 正常使用
└──────────────┘
```

---

## 实现步骤

### Phase 1：基础架构（第1-2天）

#### 步骤1.1：创建数据模型
- [ ] 创建 `User.java` 实体类
- [ ] 创建 `LoginRequest.java` 和 `LoginResponse.java`
- [ ] 创建 `Result.java` 通用返回类

#### 步骤1.2：数据库实现
- [ ] 创建 `UserDatabase.java`（SQLiteOpenHelper）
- [ ] 实现用户表的CRUD操作
- [ ] 添加数据库版本管理

#### 步骤1.3：加密工具
- [ ] 创建 `EncryptionHelper.java`
- [ ] 实现密码哈希（BCrypt或PBKDF2）
- [ ] 实现敏感数据加密

### Phase 2：业务逻辑（第3-4天）

#### 步骤2.1：用户管理器
- [ ] 创建 `UserManager.java`
- [ ] 实现 `register()` 注册方法
- [ ] 实现 `login()` 登录方法
- [ ] 实现用户名唯一性检查
- [ ] 实现密码强度验证

#### 步骤2.2：会话管理器
- [ ] 创建 `SessionManager.java`
- [ ] 实现会话创建和存储
- [ ] 实现登录状态检查
- [ ] 实现自动登录恢复
- [ ] 实现注销功能

#### 步骤2.3：权限守卫
- [ ] 创建 `AuthGuard.java`
- [ ] 实现滤镜权限检查
- [ ] 实现登录提示对话框
- [ ] 实现页面跳转逻辑

### Phase 3：UI实现（第5-7天）

#### 步骤3.1：登录页面
- [ ] 创建 `activity_login.xml` 布局
- [ ] 创建 `LoginActivity.java`
- [ ] 实现输入验证
- [ ] 实现登录逻辑
- [ ] 实现"记住我"功能
- [ ] 添加加载动画

#### 步骤3.2：注册页面
- [ ] 创建 `activity_register.xml` 布局
- [ ] 创建 `RegisterActivity.java`
- [ ] 实现用户名实时检查
- [ ] 实现密码强度显示
- [ ] 实现注册逻辑
- [ ] 添加协议勾选

#### 步骤3.3：用户中心
- [ ] 创建 `activity_profile.xml` 布局
- [ ] 创建 `ProfileActivity.java`
- [ ] 显示用户信息
- [ ] 实现退出登录
- [ ] 实现修改密码
- [ ] 实现头像上传（可选）

### Phase 4：集成改造（第8-9天）

#### 步骤4.1：HomeActivity改造
- [ ] 添加用户状态显示
- [ ] 添加登录/注册入口
- [ ] 添加用户下拉菜单
- [ ] 实现自动登录恢复

#### 步骤4.2：EditActivity改造
- [ ] 在 `showFilterPanel()` 中添加权限检查
- [ ] 滤镜按钮添加锁图标（未登录时）
- [ ] 点击滤镜时触发 `AuthGuard.checkFilterPermission()`
- [ ] 登录成功后刷新UI状态

#### 步骤4.3：登录提示对话框
- [ ] 创建 `dialog_login_prompt.xml` 布局
- [ ] 创建 `LoginPromptDialog.java`
- [ ] 实现对话框逻辑

### Phase 5：测试与优化（第10天）

#### 步骤5.1：功能测试
- [ ] 测试注册流程
- [ ] 测试登录流程
- [ ] 测试权限控制
- [ ] 测试自动登录
- [ ] 测试注销功能

#### 步骤5.2：边界测试
- [ ] 测试重复用户名
- [ ] 测试错误密码
- [ ] 测试网络异常（未来）
- [ ] 测试数据库异常

#### 步骤5.3：UI优化
- [ ] 统一视觉风格
- [ ] 优化动画效果
- [ ] 适配不同屏幕尺寸
- [ ] 优化加载体验

---

## 安全性考虑

### 1. 密码安全

#### 密码存储
- **禁止明文存储**：使用BCrypt或PBKDF2进行哈希
- **加盐处理**：每个密码使用独立的随机盐值
- **多次迭代**：增加破解难度（建议10000次以上）

```java
// 示例代码
public class PasswordHelper {
    public static String hashPassword(String password) {
        return BCrypt.hashpw(password, BCrypt.gensalt(12));
    }
    
    public static boolean verifyPassword(String password, String hash) {
        return BCrypt.checkpw(password, hash);
    }
}
```

#### 密码强度要求
- 最小长度：8位
- 必须包含：大小写字母、数字、特殊字符（至少3种）
- 禁止常见弱密码（123456、password等）

### 2. 会话安全

#### Session Token
- 使用UUID或JWT生成会话令牌
- 设置合理的过期时间（建议7-30天）
- 支持强制过期和刷新

#### 存储安全
- SharedPreferences使用加密模式（EncryptedSharedPreferences）
- 敏感数据不存储在普通文件中
- 使用Android Keystore存储密钥

### 3. 输入验证

#### 前端验证
- 用户名：3-20位，字母数字下划线
- 密码：8-30位，复杂度检查
- 邮箱：正则表达式验证

#### 后端验证
- 所有输入都需要二次验证
- SQL注入防护（使用参数化查询）
- XSS防护（输入过滤）

### 4. 防止攻击

#### 暴力破解防护
- 登录失败次数限制（5次）
- 失败后临时锁定（5-30分钟）
- 验证码机制（可选）

#### 其他安全措施
- 防止重放攻击（时间戳验证）
- 防止CSRF攻击（Token验证）
- 应用加固（ProGuard混淆）

---

## 后期扩展

### 阶段2：云端服务（3-6个月后）

#### 功能扩展
1. **多设备同步**
   - 项目云端存储
   - 设置同步
   - 编辑历史同步

2. **社交功能**
   - 作品分享
   - 关注/粉丝系统
   - 评论和点赞

3. **高级功能**
   - 会员系统（付费解锁高级滤镜）
   - AI滤镜（云端处理）
   - 在线协作编辑

#### 技术迁移
- **认证服务**：迁移到Firebase Authentication
- **数据存储**：迁移到Cloud Firestore
- **文件存储**：使用Cloud Storage
- **推送通知**：Firebase Cloud Messaging

### 阶段3：商业化（6-12个月后）

#### 商业模式
1. **免费版**
   - 基础滤镜（10个）
   - 每日编辑限制（5次）
   - 广告支持

2. **高级版**（订阅制）
   - 全部滤镜（50+）
   - 无限编辑
   - 无广告
   - 云端存储（10GB）
   - AI高级功能

3. **专业版**（买断制）
   - 企业级功能
   - 批量处理
   - API接口
   - 技术支持

---

## 总结

### 实现优先级

#### P0（必须）
✅ 用户注册和登录  
✅ 会话管理  
✅ 滤镜权限控制  
✅ 基本安全措施  

#### P1（重要）
⭐ 自动登录  
⭐ 密码重置  
⭐ 用户中心  
⭐ 输入验证  

#### P2（可选）
💡 社交账号登录  
💡 邮箱验证  
💡 头像上传  
💡 游客模式  

### 开发时间估算
- **Phase 1-3**：7-8天（核心功能）
- **Phase 4**：2天（集成改造）
- **Phase 5**：1天（测试优化）
- **总计**：**10-11天**（完整实现）

### 技术栈总结
- **数据库**：SQLite
- **会话存储**：EncryptedSharedPreferences
- **密码哈希**：BCrypt / PBKDF2
- **UI框架**：Material Design
- **后续扩展**：Firebase（可选）

---

## 附录

### A. 依赖库清单

```gradle
// build.gradle (app)
dependencies {
    // BCrypt密码哈希
    implementation 'org.mindrot:jbcrypt:0.4'
    
    // 加密SharedPreferences
    implementation 'androidx.security:security-crypto:1.1.0-alpha06'
    
    // Material Design
    implementation 'com.google.android.material:material:1.11.0'
    
    // 可选：Firebase（未来扩展）
    // implementation 'com.google.firebase:firebase-auth:22.3.1'
    // implementation 'com.google.firebase:firebase-firestore:24.10.1'
}
```

### B. 文件清单

#### 新增文件
```
com.example.photoshop_demo/
├── auth/                          # 认证模块
│   ├── User.java                  # 用户实体
│   ├── UserManager.java           # 用户管理器
│   ├── SessionManager.java        # 会话管理器
│   ├── AuthGuard.java             # 权限守卫
│   ├── EncryptionHelper.java      # 加密工具
│   ├── PasswordHelper.java        # 密码工具
│   └── UserDatabase.java          # 用户数据库
├── ui/
│   ├── LoginActivity.java         # 登录页面
│   ├── RegisterActivity.java      # 注册页面
│   ├── ProfileActivity.java       # 用户中心
│   └── dialog/
│       └── LoginPromptDialog.java # 登录提示对话框
└── utils/
    └── ValidationHelper.java      # 输入验证工具

res/layout/
├── activity_login.xml             # 登录布局
├── activity_register.xml          # 注册布局
├── activity_profile.xml           # 用户中心布局
└── dialog_login_prompt.xml        # 登录提示对话框布局
```

### C. 关键代码示例位置

#### EditActivity.java 修改点
```java
// 第54行附近：添加权限检查
private void showFilterPanel() {
    // 添加权限检查
    if (!AuthGuard.checkFilterPermission(this)) {
        return; // 未登录则不显示滤镜面板
    }
    
    // 原有代码...
}
```

#### HomeActivity.java 修改点
```java
// onCreate中添加
private void initUserStatus() {
    SessionManager sessionManager = new SessionManager(this);
    if (sessionManager.isLoggedIn()) {
        User user = sessionManager.getCurrentUser();
        showLoggedInUI(user);
    } else {
        showLoggedOutUI();
    }
}
```

---

**文档版本**：v1.0  
**创建日期**：2024-12-30  
**作者**：AI Assistant  
**状态**：✅ 设计完成，待实现  

---

## 下一步行动

1. **确认方案**：与团队讨论技术方案选择
2. **UI设计**：让设计师完成界面设计稿
3. **开发排期**：制定详细的开发计划
4. **开始开发**：按Phase 1-5顺序实施

**准备好开始实现了吗？** 🚀

